This is untested alpha code. A lot of work is needed to make a release.

Release:
- full unit test coverage
- accuracy/speed benchmark
 - language-specific batch runner (separate project)
 - language-specific instrumentation (constants & traces)
 - language-neutral benchmark monitor
- javadoc
- maven package
- website update, publish javadoc, new tutorial (directly on homepage)

Mono:
- migrate existing code to cross-platform Mono
- update the existing .NET code to match latest java code
- cover with NUnit tests
- language-specific batch runner & instrumentation to test accuracy
- language-neutral consistency test with java (compare two benchmarks)
- API docs, translate from XML directly to web
- nuget package
- website update, publish docs, update tutorial
